---
export const prerender = false;
import { getImage } from 'astro:assets';
import AppLayout from '../layouts/AppLayout';
const backgroundMap = {
  'bg-1': '/images/mac-background1.jpg',
  'bg-2': '/images/mac-background2.jpg',
  'bg-3': '/images/mac-background3.jpg'
};

// 2. Función para elegir un fondo aleatorio entre 'bg-1', 'bg-2' o 'bg-3'
function getRandomBackground() {
  const keys = Object.keys(backgroundMap);
  return keys[Math.floor(Math.random() * keys.length)];
}

const optimizedBackgrounds = await Promise.all(
  backgrounds.map((bg) =>
    getImage({
      src: bg,
      width: 3500,
    })
  )
);
---

<AppLayout
  client:load
  initialBg={getRandomBackground()}
  backgroundMap={backgroundMap}
/>
